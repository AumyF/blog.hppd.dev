import styled from "@emotion/styled";

export const Strong = styled.strong`
  font-weight: 600;
  background-image: linear-gradient(
    transparent 80%,
    var(--secondary) 0,
    var(--secondary) 0
  );
`;
